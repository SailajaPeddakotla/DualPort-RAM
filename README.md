# DualPort-RAM
It is about DualPort RAM where a single RAM consists for 2 ports which can read and write the data simultaneously but which can lead to issues like overwritten and unknown values.
Collision Detection signal will bw high whenever there's overwriting of the data without reading the date
